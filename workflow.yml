name: "My First Workflow"
on: "push"
jobs:
  first_job:
    runs-on: "ubuntu-latest"
    steps:
      - name: "checkout"
        uses: "actions/checkout@v3"
      - name: "print"
        run: echo "Hello, my first workflow!"
